---
kafka_basename: "kafka_{{kafka_scala_version}}-{{kafka_version}}"
kafka_archive: "{{kafka_basename}}.tgz"
kafka_url: '{{kafka_mirror}}/{{kafka_version}}/{{kafka_archive}}'
kafka_install_dir: "{{kafka_install_chdir}}/kafka"
kafka_tmp_unpack_dir: "/tmp/{{kafka_basename}}"
